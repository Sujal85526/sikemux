[project]
name = "sikemux-browser"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "browser-use[cli]==0.13.8",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "playwright>=1.62.0",
]
