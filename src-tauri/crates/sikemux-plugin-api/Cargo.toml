[package]
name = "sikemux-plugin-api"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
semver = { version = "1", features = ["serde"] }

[lints.clippy]
unwrap_used = "deny"
expect_used = "deny"
panic = "deny"
